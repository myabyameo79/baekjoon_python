from math import comb
def fac(n):
    res=1
    for i in range(1,n+1):
        res*=i
    return res
pm=[(-1)**i for i in range(18)]
for _ in range(int(input())):
    n,a,b=map(int,input().split())
    ans=0
    for i in range(b+1):
        ans+=fac(a-i)*comb(b,i)*pm[i]
    print(n,ans)
